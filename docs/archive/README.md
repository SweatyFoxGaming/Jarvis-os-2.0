# Archive

Historical files moved out of the repo root to reduce clutter — kept for reference,
not maintained, and not representative of the current system. See the top-level
`README.md` for what's actually running.

- `CONTEXT.md` — an unedited running log of past session notes (contains duplicate
  section headers and a raw pasted shell command); superseded by git history and
  `docs/architecture/ROADMAP.md`.
- `Cognitive.md` — a stray partial duplicate of `COGNITIVE_MODEL.md`.
- `operational_policies.md`, `development_constitution.md`, `execution_model.md` —
  diverged duplicate drafts of the (also historical) root-level docs of the same
  name; moved here rather than maintained as two conflicting copies.
